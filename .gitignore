node_modules
dist
.env
.DS_Store
# Python virtualenv (Replit side-effect)
.venv/
# Replit AI junk files (empty, never intentional)
brand
design
Coroutine.h
await import()
server/public
vite.config.ts.*
# WIP
LOCAL_DEV.md
# PyCharm 
.idea/workspace.xml
*.tmp
*.tar.gz
.cache
.local
.upm
*.tsbuildinfo
.tsbuildinfo
mobile/.tsbuildinfo
# Playwright
playwright-report/
test-results/
server/__tests__/e2e/.test-context.json
maestro/output
# QA test architecture (Phase 1) — run artifacts & panic marker
tests/output/
tests/PANIC
# Test-expansion (Phase 2) — claimed prompt files are transient local work-state
# (the durable record is backlog.tsv + the produced tests). See tests/plan/README.md.
tests/plan/units/
# Local-only: editor project, Expo metadata, scratch dir, device logs
.idea/
.expo/
tmp/
*.adb.log
# Ad-hoc debug screenshots belong in tmp/maestro/, never the repo root
/*.png
__pycache__/
