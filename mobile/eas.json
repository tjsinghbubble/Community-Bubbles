{
  "cli": {
    "version": ">= 16.0.0",
    "requireCommit": true
  },
  "build": {
    "development": {
      "developmentClient": true,
      "distribution": "internal"
    },
    "preview": {
      "distribution": "internal"
    },
    "production": {
      "autoIncrement": true,
      "prebuildCommand": "APP_VERSION=$(node -p \"require('./app.json').expo.version\") && npx sentry-cli releases new \"$APP_VERSION\" && (npx sentry-cli releases set-commits \"$APP_VERSION\" --auto || true)"
    }
  },
  "submit": {
    "production": {}
  }
}
