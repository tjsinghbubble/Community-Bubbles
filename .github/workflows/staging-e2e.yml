name: Staging E2E

# Runs after every merge to develop — validates the live staging server
# before anyone tests on devices. Skipped if STAGING_URL secret is not set.
on:
  push:
    branches: [develop]

jobs:
  check:
    name: Check staging URL is configured
    runs-on: ubuntu-latest
    outputs:
      enabled: ${{ steps.check.outputs.enabled }}
    steps:
      - id: check
        run: |
          if [ -z "$STAGING_URL" ]; then
            echo "enabled=false" >> "$GITHUB_OUTPUT"
            echo "STAGING_URL not set — staging E2E will be skipped."
          else
            echo "enabled=true" >> "$GITHUB_OUTPUT"
          fi
        env:
          STAGING_URL: ${{ secrets.STAGING_URL }}

  staging-e2e:
    name: API E2E tests against staging
    runs-on: ubuntu-latest
    needs: check
    if: needs.check.outputs.enabled == 'true'

    permissions:
      statuses: write

    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: "20"
          cache: npm

      - name: Install dependencies
        run: npm ci

      - name: Run Playwright E2E tests against staging server
        run: npx playwright test
        env:
          E2E_BASE_URL: ${{ secrets.STAGING_URL }}

      - name: Post result as commit status
        if: always()
        uses: actions/github-script@v7
        with:
          script: |
            const state = '${{ job.status }}' === 'success' ? 'success' : 'failure';
            const description = state === 'success'
              ? 'All E2E tests passed on staging'
              : 'E2E tests failed on staging — do not release';
            await github.rest.repos.createCommitStatus({
              owner: context.repo.owner,
              repo: context.repo.repo,
              sha: context.sha,
              state,
              description,
              context: 'staging-e2e',
            });

      - name: Upload Playwright report on failure
        if: failure()
        uses: actions/upload-artifact@v4
        with:
          name: staging-playwright-report-${{ github.run_id }}
          path: playwright-report/
          retention-days: 7
